import React, { useState } from 'react';
import { Card, CardHeader, CardContent } from '../components/@/components/ui/card';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../components/@/components/ui/select';
import { Button } from '../components/@/components/ui/button';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '../components/@/components/ui/table';
import { BarChart, Bar, PieChart, Pie, Cell, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts';

const Products = () => {
  return 0;
}

export default Products;

/**
 * Create product configurations using the materials
 * Create a lineup of current products
 * Create potential product formulations using the materials in the materials tab
 * See the 
 */